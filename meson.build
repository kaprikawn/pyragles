project( 'pyragles', 'cpp' )

sdl2_dep       = dependency( 'sdl2'      , required : true )
sdl2_mixer_dep = dependency( 'SDL2_mixer', required : true )
gles_dep       = dependency( 'glesv2'    , required : true )

subdir( 'src' )

