#!/bin/bash

let cores=`grep -c ^processor /proc/cpuinfo`

useHUD=1

clear

cmake . || exit 1

if [[ $cores -gt 1 ]]
then
  make -j${cores} || exit 1
else
  make || exit 1
fi

if [[ $useHUD -eq 1 ]]
then
  GALLIUM_HUD=".dfps;cpu0+cpu1+cpu2+cpu3;GPU-load;VRAM-usage" ./exe
else
  ./exe
fi

exit 0

